# 赛题三数据库数据结构说明

本文档说明 5.1 数据接入与索引模块涉及的各类数据库与文件型数据结构。

当前涉及四类存储：

| 存储 | 服务 / 位置 | 主要用途 |
| --- | --- | --- |
| MySQL | `scholar_search` | 元数据、查询集、gold 标注、实验记录、成本日志 |
| Elasticsearch | `saiti3_papers_v1`、`saiti3_paper_chunks_v1` | BM25 / 文本检索索引 |
| Qdrant | `saiti3_paper_chunks_v1` collection | chunk lexical sparse baseline 向量检索 |
| 本地 JSONL | `data_processed/` | 数据转换中间产物，便于导入和复现实验 |

## 1. 总体数据流

```text
PaSa / AstaBench 原始数据
  -> 转换脚本
    -> data_processed/*.jsonl
      -> MySQL: 元数据、查询、gold、评测与实验记录
      -> Elasticsearch: papers / paper_chunks 文本检索索引
      -> Qdrant: paper_chunks lexical sparse baseline 向量集合
```

检索阶段不能读取 `gold_labels` 或 `answer_arxiv_id`。这些字段只用于最终离线评测。

## 2. MySQL：scholar_search

MySQL 作为权威元数据和实验事实存储。

配置：

```text
host: 10.99.24.182
port: 48752
database: scholar_search
charset: utf8mb4
collation: utf8mb4_0900_ai_ci
```

### 2.1 datasets

记录数据集来源和 split。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | `BIGINT UNSIGNED` | 自增主键 |
| `name` | `VARCHAR(128)` | 数据集名称，如 `AutoScholarQuery` |
| `split_name` | `VARCHAR(64)` | split 名称，如 `train` / `dev` / `test` |
| `source_path` | `VARCHAR(512)` | 原始文件路径 |
| `row_count` | `BIGINT UNSIGNED` | 行数 |
| `created_at` | `TIMESTAMP` | 创建时间 |

约束：

- 主键：`id`
- 唯一键：`uk_dataset_split(name, split_name)`

### 2.2 papers

论文元数据主表。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `paper_id` | `VARCHAR(128)` | 统一论文 ID，优先格式为 `arxiv:<id>` |
| `arxiv_id` | `VARCHAR(64)` | 标准化 arXiv ID |
| `title` | `TEXT` | 论文标题 |
| `abstract` | `MEDIUMTEXT` | 摘要 |
| `year` | `INT` | 年份 |
| `published_time` | `DATE` | 发布时间 |
| `venue` | `VARCHAR(255)` | 发表 venue |
| `authors` | `JSON` | 作者列表 |
| `citation_count` | `INT` | 引用数 |
| `source` | `VARCHAR(64)` | 数据来源，如 `pasa` |
| `fulltext_key` | `TEXT` | `cs_paper_2nd.zip` 内部 key；按真实标题 slug 保留完整值 |
| `has_fulltext` | `TINYINT(1)` | 是否有全文 / section 数据 |
| `created_at` | `TIMESTAMP` | 创建时间 |
| `updated_at` | `TIMESTAMP` | 更新时间 |

约束与索引：

- 主键：`paper_id`
- 索引：`arxiv_id`、`year`、`source`

### 2.3 paper_identifiers

论文 ID 归一化映射表。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | `BIGINT UNSIGNED` | 自增主键 |
| `paper_id` | `VARCHAR(128)` | 统一论文 ID |
| `id_type` | `VARCHAR(64)` | ID 类型，如 `arxiv` / `doi` / `corpus_id` / `openalex` |
| `id_value` | `VARCHAR(255)` | ID 值 |
| `created_at` | `TIMESTAMP` | 创建时间 |

约束与索引：

- 主键：`id`
- 唯一键：`uk_identifier(id_type, id_value)`
- 外键：`paper_id -> papers.paper_id`

### 2.4 paper_chunks

论文文本切片表。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `chunk_id` | `VARCHAR(160)` | chunk 唯一 ID，格式如 `arxiv:2404.00001#chunk:0` |
| `paper_id` | `VARCHAR(128)` | 所属论文 ID |
| `chunk_index` | `INT` | 论文内 chunk 序号 |
| `chunk_type` | `VARCHAR(64)` | chunk 类型，如 `title_abstract` / `section_references` |
| `section_title` | `TEXT` | 章节标题；原始 section 标题可能较长 |
| `text` | `MEDIUMTEXT` | chunk 文本 |
| `token_estimate` | `INT` | 粗略 token 估计 |
| `source` | `VARCHAR(64)` | 数据来源 |
| `created_at` | `TIMESTAMP` | 创建时间 |

约束与索引：

- 主键：`chunk_id`
- 索引：`paper_id`、`chunk_type`
- 外键：`paper_id -> papers.paper_id`

### 2.5 queries

查询集表。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `qid` | `VARCHAR(128)` | 查询 ID |
| `dataset_name` | `VARCHAR(128)` | 数据集名称 |
| `split_name` | `VARCHAR(64)` | split 名称 |
| `query_text` | `MEDIUMTEXT` | 用户自然语言查询 |
| `published_time` | `DATE` | 查询发布时间，用于时间截断 |
| `answer_count` | `INT` | gold 论文数量 |
| `source_path` | `VARCHAR(512)` | 原始数据路径 |
| `created_at` | `TIMESTAMP` | 创建时间 |

约束与索引：

- 主键：`qid`
- 索引：`dataset_name, split_name`、`published_time`

### 2.6 gold_labels

查询对应的标准答案表。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | `BIGINT UNSIGNED` | 自增主键 |
| `qid` | `VARCHAR(128)` | 查询 ID |
| `paper_id` | `VARCHAR(128)` | gold 论文统一 ID |
| `arxiv_id` | `VARCHAR(64)` | gold arXiv ID |
| `title` | `TEXT` | gold 论文标题 |
| `label_rank` | `INT` | gold 列表中的顺序 |
| `source` | `VARCHAR(64)` | 数据来源 |
| `created_at` | `TIMESTAMP` | 创建时间 |

约束与索引：

- 主键：`id`
- 唯一键：`uk_gold_qid_paper(qid, paper_id)`
- 索引：`qid`、`paper_id`
- 外键：`qid -> queries.qid`

注意：`gold_labels` 只允许评测阶段使用，不能进入检索召回阶段。

### 2.7 eval_sets

统一评测集表。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | `BIGINT UNSIGNED` | 自增主键 |
| `dataset_name` | `VARCHAR(128)` | 数据集名称 |
| `split_name` | `VARCHAR(64)` | split 名称 |
| `qid` | `VARCHAR(128)` | 查询 ID |
| `gold_paper_ids` | `JSON` | gold 论文 ID 数组 |
| `published_time` | `DATE` | 查询发布时间 |
| `created_at` | `TIMESTAMP` | 创建时间 |

约束与索引：

- 主键：`id`
- 唯一键：`uk_eval_qid(dataset_name, split_name, qid)`
- 索引：`dataset_name, split_name`
- 外键：`qid -> queries.qid`

### 2.8 ingestion_runs

数据导入运行记录。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | `BIGINT UNSIGNED` | 自增主键 |
| `run_name` | `VARCHAR(128)` | 导入任务名称 |
| `source` | `VARCHAR(128)` | 数据来源 |
| `status` | `VARCHAR(32)` | 状态，如 `running` / `success` / `failed` |
| `config` | `JSON` | 运行配置 |
| `started_at` | `TIMESTAMP` | 开始时间 |
| `finished_at` | `TIMESTAMP` | 结束时间 |
| `stats` | `JSON` | 统计信息 |

索引：

- 主键：`id`
- 索引：`status`

### 2.9 search_runs

检索实验运行记录。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `run_id` | `VARCHAR(128)` | 检索运行 ID |
| `qid` | `VARCHAR(128)` | 查询 ID |
| `strategy` | `VARCHAR(128)` | 检索策略名称 |
| `started_at` | `TIMESTAMP` | 开始时间 |
| `finished_at` | `TIMESTAMP` | 结束时间 |
| `latency_ms` | `INT` | 端到端耗时 |
| `api_call_count` | `INT` | API 调用次数 |
| `token_input` | `INT` | 输入 token |
| `token_output` | `INT` | 输出 token |
| `config` | `JSON` | 本次运行配置 |

约束与索引：

- 主键：`run_id`
- 索引：`qid`
- 外键：`qid -> queries.qid`

### 2.10 search_results

检索结果表。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | `BIGINT UNSIGNED` | 自增主键 |
| `run_id` | `VARCHAR(128)` | 检索运行 ID |
| `qid` | `VARCHAR(128)` | 查询 ID |
| `paper_id` | `VARCHAR(128)` | 返回论文 ID |
| `rank_position` | `INT` | 排名 |
| `score` | `DOUBLE` | 检索或排序分数 |
| `source` | `VARCHAR(64)` | 结果来源，如 `es_bm25` / `qdrant` / `hybrid` |
| `reason` | `TEXT` | 排序或过滤原因 |
| `created_at` | `TIMESTAMP` | 创建时间 |

约束与索引：

- 主键：`id`
- 唯一键：`uk_run_rank(run_id, rank_position)`
- 索引：`run_id`、`qid`、`paper_id`
- 外键：`run_id -> search_runs.run_id`

### 2.11 api_call_logs

在线 API 调用日志。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | `BIGINT UNSIGNED` | 自增主键 |
| `run_id` | `VARCHAR(128)` | 关联运行 ID |
| `provider` | `VARCHAR(128)` | API 提供方，如 `semantic_scholar` |
| `endpoint` | `VARCHAR(512)` | 接口地址 |
| `request_hash` | `VARCHAR(128)` | 请求内容 hash |
| `status_code` | `INT` | HTTP 状态码 |
| `latency_ms` | `INT` | 调用耗时 |
| `created_at` | `TIMESTAMP` | 创建时间 |

索引：

- 主键：`id`
- 索引：`run_id`、`provider`

### 2.12 cost_logs

成本日志。

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `id` | `BIGINT UNSIGNED` | 自增主键 |
| `run_id` | `VARCHAR(128)` | 关联运行 ID |
| `component` | `VARCHAR(128)` | 成本来源组件 |
| `model_name` | `VARCHAR(128)` | 模型名称 |
| `token_input` | `INT` | 输入 token |
| `token_output` | `INT` | 输出 token |
| `api_call_count` | `INT` | API 调用次数 |
| `elapsed_ms` | `INT` | 耗时 |
| `created_at` | `TIMESTAMP` | 创建时间 |

索引：

- 主键：`id`
- 索引：`run_id`、`component`

## 3. Elasticsearch 索引结构

Elasticsearch 用于文本检索和 BM25 召回。

配置：

```text
url: http://10.99.24.182:32097
user: kaede
scope: saiti3_*
```

### 3.1 saiti3_papers_v1

论文级检索索引。

当前样本数据：

```text
docs.count: 3
health: green
```

字段：

| 字段 | ES 类型 | 说明 |
| --- | --- | --- |
| `paper_id` | `keyword` | 统一论文 ID |
| `arxiv_id` | `keyword` | arXiv ID |
| `title` | `text` | 论文标题，参与全文检索 |
| `title.raw` | `keyword` | 标题精确匹配 / 聚合 |
| `abstract` | `text` | 摘要 |
| `year` | `integer` | 年份 |
| `published_time` | `date` | 发布时间 |
| `venue` | `keyword` | venue |
| `authors` | `keyword` | 作者 |
| `citation_count` | `integer` | 引用数 |
| `source` | `keyword` | 来源 |
| `fulltext_key` | `keyword` | zip 内部 key |
| `has_fulltext` | `boolean` | 是否有全文 / section 数据 |

典型用途：

- 标题 + 摘要 BM25 召回。
- 按 arXiv ID 精确查找。
- 按年份、来源、是否有全文过滤。

### 3.2 saiti3_paper_chunks_v1

chunk 级检索索引。

当前样本数据：

```text
docs.count: 25
health: green
```

字段：

| 字段 | ES 类型 | 说明 |
| --- | --- | --- |
| `chunk_id` | `keyword` | chunk 唯一 ID |
| `paper_id` | `keyword` | 所属论文 ID |
| `chunk_index` | `integer` | chunk 序号 |
| `chunk_type` | `keyword` | chunk 类型 |
| `section_title` | `text` | 章节标题 |
| `section_title.raw` | `keyword` | 章节标题精确匹配 |
| `text` | `text` | chunk 内容 |
| `token_estimate` | `integer` | token 估计 |
| `source` | `keyword` | 来源 |

典型用途：

- chunk 级 BM25 召回。
- section reference 检索。
- 与 Qdrant 向量召回结果按 `chunk_id` 合并。

## 4. Qdrant Collection 结构

Qdrant 用于向量检索。当前全量导入采用 lexical sparse baseline，用于先跑通向量库隔离、过滤、回表和 hybrid 检索链路；它不是最终语义 embedding。

配置：

```text
REST: http://10.99.24.182:32333
gRPC: 10.99.24.182:32334
API key: ScholarSearchVector2026
collection: saiti3_paper_chunks_v1
```

### 4.1 saiti3_paper_chunks_v1

当前 collection 配置：

| 配置项 | 值 |
| --- | --- |
| `status` | `green` |
| `vectors` | `{}` |
| `sparse_vectors` | `text` |
| `hash dimensions` | `65536` |
| `points_count` | 当前 `2402191`，目标 `5029139`；截至 `2026-06-10 10:10 CST` 仍在远端分片导入 |
| `indexed_vectors_count` | 当前 `2402191`，目标 `5029139`；截至 `2026-06-10 10:10 CST` 仍在远端分片导入 |
| `segments_count` | `2` |
| `on_disk_payload` | `true` |
| `hnsw.m` | `16` |
| `hnsw.ef_construct` | `100` |

当前 point 结构：

```json
{
  "id": "UUID5(namespace, chunk_id)",
  "vector": {
    "text": {
      "indices": [101, 2048, 90991],
      "values": [1.0, 2.0, 1.0]
    }
  },
  "payload": {
    "chunk_id": "arxiv:2404.00001#chunk:0",
    "paper_id": "arxiv:2404.00001",
    "chunk_index": 0,
    "chunk_type": "title_abstract",
    "section_title": null,
    "source": "pasa",
    "token_estimate": 128
  }
}
```

payload 字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `chunk_id` | string | chunk 唯一 ID |
| `paper_id` | string | 论文 ID |
| `chunk_index` | integer | 论文内 chunk 序号 |
| `chunk_type` | string | chunk 类型 |
| `section_title` | string / null | 章节标题 |
| `source` | string | 数据来源 |
| `token_estimate` | integer | token 估计 |

注意：

- Qdrant REST point id 使用 UUID5，由 `chunk_id` 稳定生成；业务回表和跨库对齐使用 payload 中的 `chunk_id`。
- 当前 `text` sparse vector 是 lexical hash baseline，hash 特征空间为 `65536`。
- 接入 bge / Qwen embedding 后，建议新建 dense collection 或增加 dense vector name，并重新批量 upsert 语义向量。

## 5. data_processed JSONL 结构

位置：

```text
F:\中国研究生人工智能大赛\ScholarSearchAgent\data_ingestion_indexing\data_processed
/data/csp/data_processed
```

远端 `/data/csp/data_processed` 全量统计：

| 文件 | 行数 | 说明 |
| --- | ---: | --- |
| `queries.jsonl` | 35601 | 查询样本 |
| `gold_labels.jsonl` | 92838 | gold 标注；导入 MySQL 后按唯一键去重为 `92656` |
| `eval_sets.jsonl` | 35601 | 统一评测集 |
| `papers.jsonl` | 569432 | 论文元数据；导入 MySQL / ES 后按 `paper_id` 去重为 `569265` |
| `paper_chunks.jsonl` | 5029756 | 论文 chunk；导入 MySQL / ES 后按 `chunk_id` 去重为 `5029139` |
| `conversion_stats.json` | 1 | 转换统计 |

### 5.1 queries.jsonl

字段：

```json
{
  "qid": "AutoScholarQuery_test_0",
  "dataset_name": "AutoScholarQuery",
  "split_name": "test",
  "query_text": "Can you tell me some papers about ...?",
  "published_time": "2023-10-24",
  "answer_count": 1,
  "source_path": "AutoScholarQuery/test.jsonl"
}
```

### 5.2 gold_labels.jsonl

字段：

```json
{
  "qid": "RealScholarQuery_0",
  "paper_id": "arxiv:2309.04564",
  "arxiv_id": "2309.04564",
  "title": "When Less is More: Investigating Data Pruning for Pretraining LLMs at Scale",
  "label_rank": 1,
  "source": "RealScholarQuery"
}
```

### 5.3 eval_sets.jsonl

字段：

```json
{
  "dataset_name": "RealScholarQuery",
  "split_name": "test",
  "qid": "RealScholarQuery_0",
  "gold_paper_ids": ["arxiv:2309.04564", "arxiv:2402.09668"],
  "published_time": "2024-10-01"
}
```

### 5.4 papers.jsonl

字段：

```json
{
  "paper_id": "arxiv:2404.00001",
  "arxiv_id": "2404.00001",
  "title": "Uso de herramientas digitales matematicas en la Educacion Secundaria",
  "abstract": "...",
  "year": 2024,
  "published_time": null,
  "venue": null,
  "authors": [],
  "citation_count": null,
  "source": "pasa",
  "fulltext_key": "usodeherramientasdigitalesmatematicasenlaeducacionsecundaria",
  "has_fulltext": true
}
```

### 5.5 paper_chunks.jsonl

字段：

```json
{
  "chunk_id": "arxiv:2404.00001#chunk:0",
  "paper_id": "arxiv:2404.00001",
  "chunk_index": 0,
  "chunk_type": "title_abstract",
  "section_title": null,
  "text": "Title: ...\nAbstract: ...",
  "token_estimate": 256,
  "source": "pasa"
}
```

## 6. ID 对齐规则

统一 ID 优先级：

1. arXiv ID：`arxiv:<normalized_arxiv_id>`
2. DOI：后续可扩展为 `doi:<normalized_doi>`
3. Semantic Scholar corpusId：后续可扩展为 `s2:<corpus_id>`
4. OpenAlex ID：后续可扩展为 `openalex:<id>`
5. title hash：无法获得标准 ID 时使用 `title:<sha1_prefix>`

当前 PaSa 数据主要使用 arXiv ID，因此 `paper_id` 默认形如：

```text
arxiv:2404.00001
```

## 7. 数据库之间的关联关系

```text
MySQL papers.paper_id
  = Elasticsearch saiti3_papers_v1.paper_id
  = Elasticsearch saiti3_paper_chunks_v1.paper_id
  = Qdrant payload.paper_id

MySQL paper_chunks.chunk_id
  = Elasticsearch saiti3_paper_chunks_v1.chunk_id
  = Qdrant point id
  = Qdrant payload.chunk_id

MySQL queries.qid
  = MySQL gold_labels.qid
  = MySQL eval_sets.qid
  = MySQL search_runs.qid
  = MySQL search_results.qid
```

## 8. 后续扩展建议

- 为 MySQL 增加只读/读写分离的业务账户，避免长期使用 `root`。
- 为 Qdrant 写入 embedding 后，增加 payload index，例如 `paper_id`、`chunk_type`、`year`。
- 为 Elasticsearch 增加 alias：`saiti3_papers_current`、`saiti3_chunks_current`。
- 如果引入在线 API，扩展 `paper_identifiers` 保存 DOI、Semantic Scholar corpusId、OpenAlex ID。
- 如果引入 reranker，扩展 `search_results.reason` 或新增 `rerank_logs` 保存精排解释和分数。
